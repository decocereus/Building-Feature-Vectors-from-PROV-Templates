# run - here i will call everything and make the program run
# typeExtractor - this will get the types from the template, i can read the template and get the types
# getTypeFrequency - this will create a hashtable where type is key and its frequency is value
# createFeatureVector - Build feature vector
# buildSVM - build svm from feature vector
# machineLearningAlgo - call this after the svm has been created
